#ifndef COLLISIONS
#define COLLISIONS

#include "Extern Vars/extern.h"
#include "Extern Vars/Vec2/vec2.h"

namespace pong {

	namespace elements {
		/*
		struct Ball
		{
		public:
			Ball();
			~Ball();
		private:
			int aux;
		};

		enum class COL
		{
			CIRCLE,
			RECTANGLE,
			POINT
		};

		struct Collider
		{
		public:
			Collider(){}
			Collider(Circle c)
			{
				circ = c;
				lockCol = COL::CIRCLE;
			}
			Collider(Rectangle r)
			{
				rec = r;
				lockCol = COL::RECTANGLE;
			}
			Collider(Vec2 p)
			{
				point = p;
				lockCol = COL::RECTANGLE;
			}
			void GetCollider()
			{

			}
		private:
			
			COL lockCol;
			Circle circ;
			Rectangle rec;
			Vec2 point;
		};

		struct Circle
		{
		public:
			Vec2 center;
			float radius;
			Color color;
			void Draw()
			{
				DrawCircle(center.x, center.y, radius, color);
			}
		};
		*/
	}

}

#endif